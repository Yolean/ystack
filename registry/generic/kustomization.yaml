resources:
- service-builds.yaml
- service-prod-placeholder.yaml
- deployment.yaml
patchesStrategicMerge:
- debug-endpoint-readiness-probe.yaml
configMapGenerator:
- name: registry-config
  files:
  - config.yml
secretGenerator:
- name: registry
  literals:
  - http_secret=TODO_GENERATE_RANDOM_SECRET
