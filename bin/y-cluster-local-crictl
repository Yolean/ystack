#!/usr/bin/env bash
[ -z "$DEBUG" ] || set -x
set -eo pipefail

PROVISIONER=$(y-cluster-local-detect)

case "$PROVISIONER" in
  k3d)
    docker exec -i k3d-ystack-server-0 crictl "$@"
    ;;
  multipass)
    multipass exec ystack-master -- sudo k3s crictl "$@"
    ;;
  lima)
    limactl shell ystack sudo k3s crictl "$@"
    ;;
esac
