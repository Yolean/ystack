apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: y-kustomize
  labels:
    yolean.se/module-part: gateway
spec:
  parentRefs:
  - name: ystack
  hostnames:
  - y-kustomize.ystack.svc.cluster.local
  rules:
  - backendRefs:
    - name: y-kustomize
      port: 80
