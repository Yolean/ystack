apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: ystack
  labels:
    yolean.se/module-part: gateway
spec:
  gatewayClassName: y-cluster
  listeners:
  - name: http
    protocol: HTTP
    port: 80
    allowedRoutes:
      namespaces:
        from: All
