apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: prometheus-now
  labels:
    yolean.se/module-part: gateway
spec:
  parentRefs:
  - name: ystack
    namespace: ystack
  hostnames:
  - prometheus-now.monitoring.svc.cluster.local
  rules:
  - backendRefs:
    - name: prometheus-now
      port: 9090
