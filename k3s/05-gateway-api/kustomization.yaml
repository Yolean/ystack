apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Gateway API CRDs are managed by k3s via the traefik-crd HelmChart.
# We only configure traefik to enable the Gateway provider.
resources:
- traefik-gateway-provider.yaml
