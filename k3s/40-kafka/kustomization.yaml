# yaml-language-server: $schema=https://json.schemastore.org/kustomization.json
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kafka
resources:
- ../../kafka/base
- ../../kafka/topic-job-rbac
components:
- ../../kafka/redpanda-image
